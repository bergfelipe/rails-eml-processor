<h1>Resultado do Processamento</h1>

  <p><strong>ID do Arquivo:</strong> <%= @arquivo.id %></p>
  <p><strong>Nome do arquivo:</strong> <%= @arquivo.arquivo_email.filename %></p>
  
  <hr>
  
  <h2>Status</h2>
  <p><%= @log&.status || "Nenhum log encontrado" %></p>
  
  <h2>Mensagem</h2>
  <p><%= @log&.mensagem || "Sem mensagem" %></p>
  
  <hr>
  
  <h2>Log completo</h2>
  <pre><%= JSON.pretty_generate(@log&.dados_extraidos || {}) %></pre>
  
  <hr>
  
  <h2>Cliente</h2>
  <% if @cliente %>
    <p><strong>Nome:</strong> <%= @cliente.nome %></p>
    <p><strong>Email:</strong> <%= @cliente.email %></p>
    <p><strong>Telefone:</strong> <%= @cliente.telefone %></p>
  
    <%= link_to "Ver cliente completo", clientes_index_path(@cliente), class: "btn btn-info" %>
  <% else %>
    <p>Nenhum cliente criado.</p>
  <% end %>
  
  <hr>
  
  <%= link_to "Voltar", arquivos_emails_path, class: "btn btn-secondary" %>
  